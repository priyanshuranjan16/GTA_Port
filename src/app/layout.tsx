import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/ThemeProvider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Deathblade | Portfolio ",
  description: "A GTA-themed portfolio showcasing projects, skills, and experience of a passionate developer and gamer.",
  keywords: ["portfolio", "developer", "BTech", "gamer", "GTA", "web developer"],
  authors: [{ name: "Player One" }],
  openGraph: {
    title: "GTA Portfolio | Final Year BTech Student",
    description: "A GTA-themed portfolio showcasing projects in a gaming style",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
